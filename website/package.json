{
  "name": "emulate-docs",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "packageManager": "bun@1.3.14",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "sync:services": "bun scripts/sync-services.ts",
    "deploy": "astro build && wrangler pages deploy dist"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.36.0",
    "astro": "^5.6.0",
    "sharp": "^0.34.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
